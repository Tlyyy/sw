# 图片目录说明

当前只在根目录保留这四类：

- `原始截图/`：正式截图库，按 `角色/资料类型/日期` 存放。
- `识别记录/`：人工识别记录，按角色拆成 `宠物.md` 和 `宝石.md`。
- `技能库/`：公共技能图标素材，本次不迁移。
- `旧目录/`：迁移前的旧截图目录，只做兼容和备份，不再新增。

后续新增截图优先放这里：

```text
图片/原始截图/{角色}/{宠物|宝石}/{YYYY-MM-DD}/
图片/原始截图/公共/宝石行情/{YYYY-MM-DD}/
```

角色目录示例：`FC`、`LG1`、`LG2`、`MYT`、`PT`。
